{% block _User_roles %}

{# Boucle à travers les rôles de l'utilisateur #}
{% for role in field.value %}
    {% if role == "ROLE_USER" %}
        Membre
    {% elseif role == "ROLE_ADMIN" %}
        Admin
    {% endif %}
{% endfor %}

{% endblock %}