{% extends 'base.html.twig' %}

{% block title %}Bienvenue
{% endblock %}

{% block body %}
	<div style="height: 100vh;" class="bg-dark">
		<div class=" d-flex align-items-center justify-content-between px-5 g-3" style="height: 90vh;">
			<h1 class="text-center text-light ">Bonjour
				{% if app.user %}
					{{ app.user.email }}
				{% endif %}
			</h1>
			{% if is_granted("ROLE_ADMIN") %}
				<a href="{{ path('admin') }}" class="btn btn-lg btn-light text-center">Gestion Admin</a>
			{% endif %}

		</div>
		<div class=" d-flex justify-content-end me-5">
			{% if app.user %}
				<a href="{{ path('app_logout') }}" class="btn btn-lg btn-success">Déconnexion</a>
			{% endif %}
		</div>

	</div>


{% endblock %}
